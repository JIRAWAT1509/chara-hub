spring.application.name=chara-hub-api

# Copy this file to application-local.properties for local backend development.
# Do not commit application-local.properties.

# Supabase project config
app.supabase.url=${SUPABASE_URL}
app.supabase.publishable-key=${SUPABASE_PUBLISHABLE_KEY}

# Future database config.
# Enable these only when Spring Data/JDBC/JPA integration is added intentionally.
# spring.datasource.url=${SUPABASE_DB_JDBC_URL}
# spring.datasource.username=${SUPABASE_DB_USERNAME}
# spring.datasource.password=${SUPABASE_DB_PASSWORD}

